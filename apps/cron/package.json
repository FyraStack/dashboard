{
	"name": "stack-dashboard-cron",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"check": "tsc --noEmit",
		"cf:deploy": "wrangler deploy --env=\"\"",
		"cf:deploy:preview": "wrangler deploy --env preview",
		"cf:typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "4.20260629.1",
		"typescript": "6.0.3",
		"wrangler": "4.105.0"
	}
}
